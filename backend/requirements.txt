fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.*
pydantic-settings==2.*
SQLAlchemy[asyncio]==2.0.*
aiosqlite==0.20.*
alembic==1.13.*
argon2-cffi==23.*
Pillow==10.*
python-jose[cryptography]==3.3.*
# AES-GCM + HKDF for at-rest encryption of provider api_keys (app/utils/crypto.py).
# python-jose[cryptography] already pulls this in transitively, but we depend
# on it directly so we pin it explicitly.
cryptography>=42,<46
httpx==0.27.*
# Starlette pulls multipart in lazily for ``UploadFile`` parsing; FastAPI's
# Form() and request.form() both need it installed. PR-13's POST /api/jobs
# is the first multipart route we own; older PRs didn't depend on it.
python-multipart==0.0.*

# --- test-only ---
# Kept in the same file so the dev image runs the suite without a separate
# requirements-dev.txt. They add a few MB but no production attack surface.
pytest==8.*
pytest-asyncio==0.24.*
