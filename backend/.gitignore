.venv/
__pycache__/
*.py[cod]
*.db
*.db-journal
.env
.env.local
# Real upstream API keys for the optional `pytest -m real` suite.
# Never commit these — they are billable credentials.
tests/real/.env.real
